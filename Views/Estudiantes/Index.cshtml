@model IEnumerable<SistemaGestionEscuelas.Models.Estudiante>

@{
    ViewData["Title"] = "Estudiantes";
}

<h1 class="mb-4 text-primary">Lista de Estudiantes</h1>

<div class="mb-3">
    <a class="btn btn-success" asp-action="Create">
        <i class="bi bi-plus-lg"></i> Agregar Estudiante
    </a>
</div>

<div class="table-responsive shadow-sm rounded">
    <table class="table table-hover align-middle">
        <thead class="table-primary text-center">
            <tr>
                <th>Nombre</th>
                <th>Apellido</th>
                <th>Fecha de Nacimiento</th>
                <th>Grado</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var estudiante in Model)
            {
                <tr class="text-center">
                    <td>@estudiante.Nombre</td>
                    <td>@estudiante.Apellido</td>
                    <td>@estudiante.FechaNacimiento.ToShortDateString()</td>
                    <td>@estudiante.Grado</td>
                    <td>
                        <a class="btn btn-sm btn-primary me-1" asp-action="Edit" asp-route-id="@estudiante.EstudianteId">
                            <i class="bi bi-pencil-square"></i> Editar
                        </a>
                        <a class="btn btn-sm btn-info me-1" asp-action="Details" asp-route-id="@estudiante.EstudianteId">
                            <i class="bi bi-eye"></i> Detalles
                        </a>
                        <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@estudiante.EstudianteId">
                            <i class="bi bi-trash"></i> Eliminar
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<!-- Iconos de Bootstrap -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">